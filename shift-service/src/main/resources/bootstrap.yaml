spring:
  application:
    name: shift-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 5

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/